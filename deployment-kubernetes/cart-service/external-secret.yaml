# ============================================================================
# ExternalSecret: cart-service-secret
# ----------------------------------------------------------------------------
# Syncs AWS secret "shopease/development/cart-service" -> K8s Secret of
# the same name in the shopease-webapp-development namespace.
# ============================================================================
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: cart-service-secret
  namespace: shopease-webapp-development
  labels:
    app: cart-service-deployment
    app.kubernetes.io/name: cart-service
    app.kubernetes.io/part-of: shopease-webapp
    app.kubernetes.io/managed-by: external-secrets
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: aws-secrets-manager
    kind: ClusterSecretStore
  target:
    name: cart-service-secret
    creationPolicy: Owner
  dataFrom:
    - extract:
        key: shopease/development/cart-service
